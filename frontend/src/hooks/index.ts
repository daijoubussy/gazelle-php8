/**
 * Hooks Index
 *
 * Export all custom hooks
 */

export { useAuth, AuthProvider, type AuthContextType } from './useAuth';
export { useTheme, ThemeProvider, type ThemeContextType } from './useTheme';
